# QA 자동화 레포트 (통합 테스트)

> 이 템플릿을 채워 `reports/<KST타임스탬프>_qa_report.md` 로 저장하세요.
> `<...>` 부분을 실제 값으로 치환하고, 항목을 반복합니다. 수치는 `reports/.tests/summary.json` 에서 가져옵니다.

- **대상**: `<분석 대상 경로 또는 설명>`
- **분석 일시(KST)**: `<YYYY-MM-DD HH:MM>`
- **모드**: `<full | generate-only | run-only>`
- **테스트 레벨**: 통합(Integration)
- **사용 프레임워크**: `<pytest, Jest, JUnit 등 실제 사용한 것>`

---

## 1. 요약 (Executive Summary)

핵심 결론을 두괄식으로 작성합니다. 가장 시급한 결함과 전반적 품질 상태를 먼저 명시합니다.

| 항목 | 값 |
|------|----|
| 식별한 서비스 수 | 0 |
| 생성한 통합 테스트 수 | 0 |
| 실행한 테스트 수 (기존 + 생성) | 0 |
| 실행 결과 (통과/실패/에러/스킵) | 0 / 0 / 0 / 0 |
| 확정 결함 수 (+ 미확정) | 0 (+0) |
| 잠재 위험 수 | 0 |

### 결함/위험 등급 분포

| 등급 | 건수 |
|------|------|
| Blocker | 0 |
| Critical | 0 |
| Major | 0 |
| Minor | 0 |
| Risk | 0 |

---

## 2. 분석 범위 / 제외 / 가정

정직성 원칙: 무엇을 보았고, 무엇을 보지 않았으며, 어떤 가정 위에서 판단했는지 명시합니다.

- **분석 범위**: `<예: input/svc-a 전체, input/svc-b 의 orders 모듈>`
- **제외한 영역과 사유**: `<예: input/legacy — 빌드 불가; admin UI — 통합 지점 없음>`
- **가정**: `<예: PostgreSQL 대신 SQLite 로 대체 (JSONB 컬럼 미검증); 외부 결제 API 는 목으로 대체>`
- **실행 환경**: `<OS, 언어 버전, Docker 유무, 사용한 대체 수단>`

---

## 3. 서비스 / 통합 지점 인벤토리

| 서비스/모듈 | 진입점 | 통합 지점(DB/외부API/큐 등) | 기존 테스트 |
|-------------|--------|------------------------------|-------------|
| `<이름>` | `<라우트/핸들러>` | `<예: PostgreSQL, 결제 API>` | `<있음(N개)/없음>` |

---

## 4. 생성한 통합 테스트

| 테스트 ID | 대상 서비스 | 시나리오 유형 | 테스트 함수명 | 위치(파일) |
|-----------|-------------|---------------|---------------|------------|
| T-001 | `<서비스>` | `<행복경로/경계/실패시나리오/무결성>` | `test_<...>` | `<파일경로>` |

> `input/` 은 git 무시 대상입니다. 위 파일들을 원본 저장소로 복사해야 보존됩니다.

---

## 5. 테스트 실행 결과

- 실행 명령: `tools/run_tests.sh <대상경로>`
- 집계 출처: `reports/.tests/summary.md`
- 실행하지 못한 프로젝트: `<runs.tsv 의 runner-missing / deps-missing / no-test-script / timeout 항목과 사유>`

| 테스트 ID / 함수명 | 결과 | 원인 분류 | 비고 |
|--------------------|------|-----------|------|
| T-001 `test_<...>` | 통과 | — | |
| T-002 `test_<...>` | 실패 | `<서비스 결함 / 테스트 결함(수정 후 재실행) / 환경 문제 / 플래키>` | `<실패 메시지 요약>` |

> 실행하지 못했다면, 무엇이 없어서 못 돌렸는지(예: Docker 미설치)와 정적 분석으로 대체한 범위를 명시합니다.
> "테스트 결함"으로 분류해 수정한 경우, 무엇을 잘못 가정했고 어떻게 고쳤는지 한 줄로 남깁니다.

---

## 6. 발견된 결함 (Defects)

원인 분류가 "서비스 결함"이고 재실행으로 재현이 확인된 항목만 여기에 적습니다. 확신이 없으면 제목에 `(미확정)`을 붙입니다.

### [D-001] `<결함 제목>`

- **등급**: `<Blocker/Critical/Major/Minor>`
- **근거**: `<실패한 테스트 ID + 함수명, 또는 파일:라인>`
- **재현 조건**: `<어떤 입력/상태에서 발생하는가>`
- **기대 vs 실제**: `<기대 결과> / <실제 결과>`
- **재실행 확인**: `<N회 실행, N회 동일 실패>`
- **추정 원인**: `<코드상 원인, 파일:라인>`
- **권장 조치**: `<수정 방향>`

---

## 7. 잠재 위험 (Risks)

### [R-001] `<위험 제목>`

- **등급**: Risk
- **유형**: `<미검증 경로 / 동시성 / 데이터 무결성 / 외부의존성 처리 / 계약 불일치 / 플래키 / 자격증명 노출>`
- **근거**: `<파일:라인 또는 관찰 내용>`
- **영향**: `<무엇이 잘못될 수 있는가>`
- **권장 조치**: `<완화 방법: 테스트 추가, 타임아웃/재시도, 트랜잭션 경계 등>`

---

## 8. 커버리지 공백

테스트가 닿지 못한 통합 경로/분기를 정리합니다. "환경 문제"로 분류된 실패도 여기에 포함합니다.

| 영역 | 미검증 내용 | 사유 |
|------|-------------|------|
| `<서비스/경로>` | `<예: 결제 실패 롤백 경로>` | `<예: 외부 결제 API 모킹 필요 / Docker 미설치>` |

---

## 9. 우선 개선 로드맵

| 순위 | 항목 | 등급 | 예상 난이도 | 권장 시점 |
|------|------|------|-------------|-----------|
| 1 | `<D-001 제목>` | Blocker | 낮음 | 즉시 |

---

## 10. 재현 / 실행 방법

사용자가 직접 테스트를 돌릴 수 있도록 단계별 명령을 적습니다.

```bash
# 예시
cd input/<서비스>
pip install -r requirements.txt
pytest tests/integration -q

# 또는 이 repo 의 러너로 전체 실행 + 집계
tools/run_tests.sh input/<서비스>
```
