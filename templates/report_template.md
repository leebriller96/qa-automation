# QA 자동화 레포트 (통합 테스트)

> 이 템플릿을 채워 `reports/<KST타임스탬프>_qa_report.md` 로 저장하세요.
> `<...>` 부분을 실제 값으로 치환하고, 항목을 반복합니다.

- **대상**: `<분석 대상 경로 또는 설명>`
- **분석 일시(KST)**: `<YYYY-MM-DD HH:MM>`
- **모드**: `<full | generate-only | run-only>`
- **테스트 레벨**: 통합(Integration)
- **사용 프레임워크**: `<pytest, Jest, JUnit 등 실제 사용한 것>`

---

## 1. 요약 (Executive Summary)

핵심 결론을 두괄식으로 작성합니다. 가장 시급한 결함과 전반적 품질 상태를 먼저 명시합니다.

| 항목 | 값 |
|------|----|
| 식별한 서비스 수 | 0 |
| 생성한 통합 테스트 수 | 0 |
| 실행 결과 (통과/실패/에러) | 0 / 0 / 0 |
| 발견 결함 수 | 0 |
| 잠재 위험 수 | 0 |

### 결함/위험 등급 분포

| 등급 | 건수 |
|------|------|
| Blocker | 0 |
| Critical | 0 |
| Major | 0 |
| Minor | 0 |
| Risk | 0 |

---

## 2. 서비스 / 통합 지점 인벤토리

| 서비스/모듈 | 진입점 | 통합 지점(DB/외부API/큐 등) | 기존 테스트 |
|-------------|--------|------------------------------|-------------|
| `<이름>` | `<라우트/핸들러>` | `<예: PostgreSQL, 결제 API>` | `<있음/없음>` |

---

## 3. 생성한 통합 테스트

| 테스트 ID | 대상 서비스 | 시나리오 | 위치(파일) |
|-----------|-------------|----------|------------|
| T-001 | `<서비스>` | `<행복경로/경계/실패시나리오>` | `<파일경로>` |

---

## 4. 테스트 실행 결과

- 실행 명령: `<예: pytest input/svc -q>`
- 환경: `<DB 컨테이너, 인메모리 등 / 또는 "실행 불가: 사유">`

| 테스트 ID | 결과 | 비고 |
|-----------|------|------|
| T-001 | 통과/실패/에러 | `<실패 시 원인 요약>` |

> 실행하지 못했다면, 무엇이 없어서 못 돌렸는지(예: Docker 미설치)와 정적 분석으로 대체한 범위를 명시합니다.

---

## 5. 발견된 결함 (Defects)

### [D-001] `<결함 제목>`

- **등급**: `<Blocker/Critical/Major/Minor>`
- **근거**: `<실패한 테스트 ID 또는 파일:라인>`
- **재현 조건**: `<어떤 입력/상태에서 발생하는가>`
- **기대 vs 실제**: `<기대 결과> / <실제 결과>`
- **추정 원인**: `<코드상 원인>`
- **권장 조치**: `<수정 방향>`

---

## 6. 잠재 위험 (Risks)

### [R-001] `<위험 제목>`

- **등급**: Risk
- **유형**: `<미검증 경로 / 동시성 / 데이터 무결성 / 외부의존성 처리 / 계약 불일치 / 플래키>`
- **근거**: `<파일:라인 또는 관찰 내용>`
- **영향**: `<무엇이 잘못될 수 있는가>`
- **권장 조치**: `<완화 방법: 테스트 추가, 타임아웃/재시도, 트랜잭션 경계 등>`

---

## 7. 커버리지 공백

테스트가 닿지 못한 통합 경로/분기를 정리합니다.

| 영역 | 미검증 내용 | 사유 |
|------|-------------|------|
| `<서비스/경로>` | `<예: 결제 실패 롤백 경로>` | `<예: 외부 결제 API 모킹 필요>` |

---

## 8. 우선 개선 로드맵

| 순위 | 항목 | 등급 | 예상 난이도 | 권장 시점 |
|------|------|------|-------------|-----------|
| 1 | `<D-001 제목>` | Blocker | 낮음 | 즉시 |

---

## 9. 재현 / 실행 방법

사용자가 직접 테스트를 돌릴 수 있도록 단계별 명령을 적습니다.

```bash
# 예시
cd input/<서비스>
pip install -r requirements.txt
pytest tests/integration -q
```
